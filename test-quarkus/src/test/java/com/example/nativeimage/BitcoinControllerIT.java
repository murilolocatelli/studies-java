package com.example.nativeimage;

import com.example.controller.BitcoinControllerTest;
import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class BitcoinControllerIT extends BitcoinControllerTest {

    // Execute the same tests but in native mode.
}
